'on':
  push:
    branches:
      - "**"
  schedule:
    - cron: 0 16 * * *
  workflow_dispatch:  
name: broken links?
jobs:
  linkChecker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Link Checker
        uses: lycheeverse/lychee-action@v2.0.2
        with:
          fail: true
